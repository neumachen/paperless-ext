# Deployment

Deployment examples belong here after an extension's runtime and packaging are selected. Keep environment-specific secrets outside the repository and pin container images by immutable version or digest.
