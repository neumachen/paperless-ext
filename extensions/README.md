# Extensions

Place each independently buildable extension in its own directory:

```text
extensions/
└── filename-normalizer/
```

An extension should own its source, tests, dependency lockfiles, container packaging, and local documentation. The repository intentionally does not mandate one programming language.
